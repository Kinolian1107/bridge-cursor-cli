{
  "name": "cursor-bridge",
  "version": "1.2.0",
  "description": "OpenAI-compatible API proxy for Cursor CLI — use any OpenAI-compatible client to call Cursor AI models",
  "type": "module",
  "main": "cursor-bridge.mjs",
  "scripts": {
    "start": "node cursor-bridge.mjs",
    "dev": "node --watch cursor-bridge.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
